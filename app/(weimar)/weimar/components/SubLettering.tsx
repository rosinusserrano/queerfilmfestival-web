import { wonder } from "@/app/fonts/fonts";

export default function SubLettering() {
	return (
		<center>
			<span
				className={`${wonder.className} text-white text-shadow-outline-light-pink sticky top-0`}
				style={{
					fontSize: "6vw",
				}}
			>
				Queer Film Festival Weimar
			</span>
		</center>
	);
}
